#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='rfc',
      version='1.0',
      description='Robust Fair Clustering suite',
      author='anon',
      author_email='anon',
      url='',
      packages=find_packages(),
     )